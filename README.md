# City Project Backend

FastAPI backend for the City AI project with NPC logic, WebSocket, relations, weather, elections, and more.

## Setup Locally
1. Install dependencies:
   ```bash
   pip install -r requirements.txt